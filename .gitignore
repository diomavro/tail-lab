# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.import_linter_cache/
htmlcov/
.coverage
*.coverage.*

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# The local lakehouse — bronze/silver/gold parquet, never committed.
data/

# Editors / OS
.vscode/
.DS_Store
.env
.claude/worktrees/

# Operational logs from the local systemd stand-ins for the chain sweep.
# (.chain-sweep-manual.log predates this and is already tracked; gitignore
# does not untrack it. Removing it from the index is a separate decision --
# every green main auto-deploys here, so committed logs ship.)
.chain-sweep-*.log
.chain-sweep-FAILED
.chain-sweep-FAILED.tmp.*
.daily-refresh-FAILED.tmp.*
.daily-refresh.log
.daily-refresh.log.1
.daily-refresh.lock
.daily-refresh-FAILED
